/*
 * This file is generated by jOOQ.
 */
package kz.survey.library.jooq.survey.ddl.survey.tables.pojos;


import java.io.Serializable;
import java.util.UUID;

import kz.survey.library.jooq.survey.ddl.survey.enums.Gender;


@SuppressWarnings({ "all", "unchecked", "rawtypes", "this-escape" })
public record Respondents(
    Long id,
    UUID uuid,
    Integer age,
    Gender gender,
    Double avgScreenTimeHours
) implements Serializable {

    private static final long serialVersionUID = 1L;


    @Override
    public boolean equals(Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        final Respondents other = (Respondents) obj;
        if (this.id == null) {
            if (other.id != null)
                return false;
        }
        else if (!this.id.equals(other.id))
            return false;
        if (this.uuid == null) {
            if (other.uuid != null)
                return false;
        }
        else if (!this.uuid.equals(other.uuid))
            return false;
        if (this.age == null) {
            if (other.age != null)
                return false;
        }
        else if (!this.age.equals(other.age))
            return false;
        if (this.gender == null) {
            if (other.gender != null)
                return false;
        }
        else if (!this.gender.equals(other.gender))
            return false;
        if (this.avgScreenTimeHours == null) {
            if (other.avgScreenTimeHours != null)
                return false;
        }
        else if (!this.avgScreenTimeHours.equals(other.avgScreenTimeHours))
            return false;
        return true;
    }

    @Override
    public int hashCode() {
        final int prime = 31;
        int result = 1;
        result = prime * result + ((this.id == null) ? 0 : this.id.hashCode());
        result = prime * result + ((this.uuid == null) ? 0 : this.uuid.hashCode());
        result = prime * result + ((this.age == null) ? 0 : this.age.hashCode());
        result = prime * result + ((this.gender == null) ? 0 : this.gender.hashCode());
        result = prime * result + ((this.avgScreenTimeHours == null) ? 0 : this.avgScreenTimeHours.hashCode());
        return result;
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder("Respondents (");

        sb.append(id);
        sb.append(", ").append(uuid);
        sb.append(", ").append(age);
        sb.append(", ").append(gender);
        sb.append(", ").append(avgScreenTimeHours);

        sb.append(")");
        return sb.toString();
    }
}
